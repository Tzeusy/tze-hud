interface:
  display_name: "User Test"
  short_description: "Build+deploy Windows exe over SSH, then zone publish test flow"
  default_prompt: "Run the user-test automated cross-machine HUD flow using hud-user and a full app exe: verify hud-user SSH key auth, deploy provided .exe to windows-host.example via SSH/SCP with auto task/direct launch fallback, verify MCP HTTP is reachable, then publish configurable zone messages."
